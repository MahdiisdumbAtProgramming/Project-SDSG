let clickCount=0;let isCounting=!1;let timer;let resultDisplay;let timerDisplay;let countdown;let timerDuration=5;document.addEventListener('DOMContentLoaded',()=>{const checkButton=document.querySelector('.CHECK');resultDisplay=document.getElementById('result');timerDisplay=document.getElementById('timerDisplay');checkButton.addEventListener('click',()=>{if(!isCounting){startCPSCheck()}else{clickCount++}});document.addEventListener('keydown',(event)=>{if(event.key==='Enter'){event.preventDefault()}})});function startCPSCheck(){clickCount=0;isCounting=!0;timerDisplay.textContent=`Timer: ${timerDuration}`;timer=setTimeout(()=>{isCounting=!1;const cps=(clickCount/timerDuration).toFixed(2);alert(`Your CPS: ${cps}`);clearInterval(countdown)},timerDuration*1000);let timeLeft=timerDuration;countdown=setInterval(()=>{timeLeft--;timerDisplay.textContent=`Timer: ${timeLeft}`;if(timeLeft<=0){clearInterval(countdown)}},1000)}