const canvas=document.getElementById('gameCanvas');const ctx=canvas.getContext('2d');const resSound=new Audio('./revive.mp3');const bulletImage=new Image();bulletImage.src='./Crowbar.png';const bulletImage1=new Image();bulletImage1.src='./bullet.png';const enemyImage=new Image();enemyImage.src='Headcrab.png';const killSound=new Audio('./kill.mp3');let assetsLoaded=0;bulletImage.onload=()=>{assetsLoaded++;if(assetsLoaded===2)startGame();};enemyImage.onload=()=>{assetsLoaded++;if(assetsLoaded===2)startGame();};const players=[{x:50,y:canvas.height-60,width:40,height:40,speed:10,bullets:[],alive:!0,invincible:!1,invincibleTime:0,color:'#fb7e14',defaultColor:'#fb7e14',controls:{up:'ArrowUp',down:'ArrowDown',left:'ArrowLeft',right:'ArrowRight',shoot:' '},score:0},{x:100,y:canvas.height-60,width:40,height:40,speed:10,bullets:[],alive:!0,invincible:!1,invincibleTime:0,color:'#5a4e3c',defaultColor:'#5a4e3c',controls:{up:'w',down:'s',left:'a',right:'d',shoot:'e'},score:0}];const keys={};const enemies=[];let totalScore=0;let highScore=localStorage.getItem('highScore')||0;let paused=!1;function startGame(){update()}
function drawPlayer(player){ctx.fillStyle=player.invincible?'rgba(255,255,255,0.5)':player.color;ctx.fillRect(player.x,player.y,player.width,player.height);ctx.fillStyle='#fff';ctx.font='16px Arial';ctx.fillText(`Score: ${player.score}`,player.x,player.y-10)}
function drawHighScore(){ctx.fillStyle='#fff';ctx.font='20px Arial';ctx.textAlign='center';ctx.fillText(`High Score: ${highScore}`,canvas.width/2,30)}
function drawBullets(){players.forEach(player=>{player.bullets.forEach((bullet,bIndex)=>{const bulletImg=player===players[1]?bulletImage1:bulletImage;ctx.drawImage(bulletImg,bullet.x,bullet.y,bullet.width,bullet.height);bullet.y-=bullet.speed;if(bullet.y+bullet.height<0){player.bullets.splice(bIndex,1)}else{enemies.forEach((enemy,eIndex)=>{if(bullet.x<enemy.x+enemy.width&&bullet.x+bullet.width>enemy.x&&bullet.y<enemy.y+enemy.height&&bullet.y+bullet.height>enemy.y){enemies.splice(eIndex,1);player.bullets.splice(bIndex,1);player.score++;totalScore++;if(totalScore>highScore){highScore=totalScore;localStorage.setItem('highScore',highScore)}
killSound.play()}})}})})}
function drawEnemies(){enemies.forEach((enemy,index)=>{ctx.drawImage(enemyImage,enemy.x,enemy.y,enemy.width,enemy.height);moveEnemy(enemy);checkCollisionWithPlayers(enemy,index);if(enemy.y>canvas.height||enemy.x>canvas.width||enemy.x+enemy.width<0){enemies.splice(index,1)}})}
function moveEnemy(enemy){const alivePlayers=players.filter(p=>p.alive);if(alivePlayers.length===0)return;let closestPlayer=null;let minDist=Infinity;alivePlayers.forEach(p=>{const dist=Math.hypot(p.x-enemy.x,p.y-enemy.y);if(dist<minDist){minDist=dist;closestPlayer=p}});if(closestPlayer){const angle=Math.atan2(closestPlayer.y-enemy.y,closestPlayer.x-enemy.x);enemy.x+=enemy.speed*Math.cos(angle);enemy.y+=enemy.speed*Math.sin(angle)}}
function checkCollisionWithPlayers(enemy,index){players.forEach((player)=>{if(player.alive&&!player.invincible&&player.x<enemy.x+enemy.width&&player.x+player.width>enemy.x&&player.y<enemy.y+enemy.height&&player.y+player.height>enemy.y){player.alive=!1;player.color='pink'}})}
function movePlayer(player){if(!player.alive)return;if(keys[player.controls.up]&&player.y>0)player.y-=player.speed;if(keys[player.controls.down]&&player.y+player.height<canvas.height)player.y+=player.speed;if(keys[player.controls.left]&&player.x>0)player.x-=player.speed;if(keys[player.controls.right]&&player.x+player.width<canvas.width)player.x+=player.speed}
function shoot(player){if(!player.alive||player.bullets.length>5)return;player.bullets.push({x:player.x+player.width/2-10,y:player.y,width:20,height:40,speed:14})}
function checkPlayerRevival(){players.forEach((player,index)=>{if(!player.alive){const other=players[1-index];if(other.alive&&other.x<player.x+player.width&&other.x+other.width>player.x&&other.y<player.y+player.height&&other.y+other.height>player.y){player.alive=!0;player.invincible=!0;player.invincibleTime=5000;resSound.play();player.color=player.defaultColor}}})}
function endGame(){alert("Game Over! Restarting...");location.reload()}
function checkGameOver(){if(!players[0].alive&&!players[1].alive){endGame()}}
function update(){ctx.clearRect(0,0,canvas.width,canvas.height);players.forEach(p=>{if(p.invincible){p.invincibleTime-=16;if(p.invincibleTime<=0){p.invincible=!1}}});players.forEach(drawPlayer);drawHighScore();drawBullets();drawEnemies();players.forEach(movePlayer);checkPlayerRevival();checkGameOver();requestAnimationFrame(update)}
document.addEventListener('keydown',(e)=>{keys[e.key]=!0;players.forEach(p=>{if(e.key===p.controls.shoot){shoot(p)}})});document.addEventListener('keyup',(e)=>{keys[e.key]=!1});setInterval(()=>{if(!paused){const enemyW=40;const enemyH=40;const x=Math.random()*(canvas.width-enemyW);const speed=Math.random()*2+1;enemies.push({x,y:0,width:enemyW,height:enemyH,speed})}},1000)