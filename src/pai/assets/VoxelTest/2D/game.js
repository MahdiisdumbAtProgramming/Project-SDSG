const canvas=document.getElementById('gameCanvas');const ctx=canvas.getContext('2d');const inventory=document.getElementById('inventory');canvas.width=window.innerWidth;canvas.height=window.innerHeight;const blockSize=50;const blocks=[];const blockTypes={dirt:'#8B4513',grass:'#228B22',stone:'#808080',wood:'#DEB887',ruby:'#Ff0000',emarald:'#00ff00',dimond:'#B9F2FF',coal:'#151716',clay:'#B66A50',purp:'#800080',shit:'#5A3D37',iron:'lightgrey',orange:'orange',echounito:'#C58615',echounit:'#01aae1',echogoalb:'#2deeff',echodisc:'white',};let cameraX=0;let cameraY=0;let mouseX=0;let mouseY=0;let selectedBlock='dirt';function saveGame(){const gameState={blocks:blocks,cameraX:cameraX,cameraY:cameraY};localStorage.setItem('gameState2D',JSON.stringify(gameState));console.log('Game saved!')}
function loadGame(){const savedState=localStorage.getItem('gameState2D');if(savedState){const gameState=JSON.parse(savedState);blocks.length=0;blocks.push(...gameState.blocks);cameraX=gameState.cameraX;cameraY=gameState.cameraY;console.log('Game loaded!')}}
function autoSave(){saveGame()}
setInterval(autoSave,30000);Object.entries(blockTypes).forEach(([type,color])=>{const blockElement=document.createElement('button');blockElement.className='block-select';blockElement.style.backgroundColor=color;blockElement.onclick=()=>{document.querySelectorAll('.block-select').forEach(el=>el.classList.remove('selected'));blockElement.classList.add('selected');selectedBlock=type};inventory.appendChild(blockElement)});for(let x=-20;x<20;x++){blocks.push({x:x*blockSize,y:300,type:'grass'})}
function breakNearestBlock(x,y){const gridX=Math.floor((x+cameraX)/blockSize)*blockSize;const gridY=Math.floor((y+cameraY)/blockSize)*blockSize;const index=blocks.findIndex(block=>block.x===gridX&&block.y===gridY);if(index!==-1){blocks.splice(index,1);autoSave()}}
function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#87CEEB';ctx.fillRect(0,0,canvas.width,canvas.height);blocks.forEach(block=>{ctx.fillStyle=blockTypes[block.type];ctx.fillRect(block.x-cameraX,block.y-cameraY,blockSize,blockSize);ctx.strokeStyle='#000000';ctx.lineWidth=2;ctx.strokeRect(block.x-cameraX,block.y-cameraY,blockSize,blockSize)});const hoverX=Math.floor((mouseX+cameraX)/blockSize)*blockSize-cameraX;const hoverY=Math.floor((mouseY+cameraY)/blockSize)*blockSize-cameraY;ctx.strokeStyle='#FFFFFF';ctx.lineWidth=2;ctx.strokeRect(hoverX,hoverY,blockSize,blockSize);requestAnimationFrame(draw)}
canvas.addEventListener('mousemove',(e)=>{mouseX=e.clientX;mouseY=e.clientY});canvas.addEventListener('click',(e)=>{const x=Math.floor((e.clientX+cameraX)/blockSize)*blockSize;const y=Math.floor((e.clientY+cameraY)/blockSize)*blockSize;if(e.button===0){const existingBlock=blocks.find(block=>block.x===x&&block.y===y);if(!existingBlock){blocks.push({x:x,y:y,type:selectedBlock});autoSave()}}});window.addEventListener('keydown',(e)=>{switch(e.key){case 'ArrowLeft':cameraX-=10;break;case 'ArrowRight':cameraX+=10;break;case 'ArrowUp':cameraY-=10;break;case 'ArrowDown':cameraY+=10;break;case 'b':breakNearestBlock(mouseX,mouseY);break}});document.addEventListener('DOMContentLoaded',()=>{const saveButton=document.getElementById('saveButton');const loadButton=document.getElementById('loadButton');saveButton.addEventListener('click',saveGame);loadButton.addEventListener('click',loadGame)});draw();let showingSaveIndicator=!1;function showSaveIndicator(){const indicator=document.createElement('div');indicator.textContent='Auto Saving...';indicator.style.cssText=`
        position: fixed;
        top: 20px;
        left: 20px;
        background: rgba(0, 0, 0, 0.7);
        color: white;
        padding: 10px 20px;
        border-radius: 5px;
        transition: opacity 0.5s;
    `;document.body.appendChild(indicator);setTimeout(()=>{indicator.style.opacity='0';setTimeout(()=>{document.body.removeChild(indicator)},500)},1500)}
function autoSave(){saveGame();showSaveIndicator()}
function saveGame(){const gameState={blocks:blocks,cameraX:cameraX,cameraY:cameraY};localStorage.setItem('gameState2D',JSON.stringify(gameState));showSaveIndicator()}