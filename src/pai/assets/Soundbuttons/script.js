function parseLeadingNumber(name){const m=name.trim().match(/^(\d+)/);return m?parseInt(m[1],10):null}
audioFiles.sort((a,b)=>{const aLower=a.toLowerCase();const bLower=b.toLowerCase();const aNum=parseLeadingNumber(aLower);const bNum=parseLeadingNumber(bLower);if(aNum!==null&&bNum!==null){if(aNum!==bNum)return aNum-bNum;const aRest=aLower.replace(/^\d+/,'').trim();const bRest=bLower.replace(/^\d+/,'').trim();return aRest.localeCompare(bRest)}
if(aNum!==null&&bNum===null){return-1}
if(aNum===null&&bNum!==null){return 1}
return aLower.localeCompare(bLower)});const container=document.getElementById('audio-container');let currentAudio=null;let activeButton=null;audioFiles.forEach(file=>{const button=document.createElement('button');button.textContent=file;button.classList.add('audio-button');const audio=new Audio(`./snd/${file}`);audio.loop=!0;button.addEventListener('click',()=>{if(currentAudio&&currentAudio!==audio){currentAudio.pause();currentAudio.currentTime=0}
audio.currentTime=0;audio.play();currentAudio=audio;if(activeButton)activeButton.classList.remove('active');button.classList.add('active');activeButton=button});container.appendChild(button)});document.getElementById('send').addEventListener('click',()=>{window.open('https://forms.gle/xAHborSZ9ii33n4n7')})