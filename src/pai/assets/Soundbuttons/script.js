const container=document.getElementById("audio-container");let currentAudio=null;function randomHue(){return Math.floor(Math.random()*360)}
audioFiles.sort((a,b)=>a.localeCompare(b));audioFiles.forEach(file=>{const card=document.createElement("div");card.className="sound-card";const img=document.createElement("div");img.className="sound-button-img";img.style.filter=`hue-rotate(${randomHue()}deg)`;const label=document.createElement("div");label.className="sound-label";label.textContent=file.replace(/\.[^/.]+$/,"");const audio=new Audio(`./snd/${file}`);audio.loop=!1;card.addEventListener("click",()=>{if(currentAudio){currentAudio.pause();currentAudio.currentTime=0}
audio.currentTime=0;audio.play();currentAudio=audio});card.appendChild(img);card.appendChild(label);container.appendChild(card)});document.getElementById("send").addEventListener("click",()=>{window.open("https://forms.gle/xAHborSZ9ii33n4n7")})