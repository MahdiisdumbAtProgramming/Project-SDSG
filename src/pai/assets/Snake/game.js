const canvas=document.getElementById('gameCanvas');const ctx=canvas.getContext('2d');const scale=20;const rows=canvas.height/scale;const cols=canvas.width/scale;let snake=[{x:10*scale,y:10*scale}];let direction={x:1,y:0};let food={};let score=0;let paused=!1;function placeFood(){food.x=Math.floor(Math.random()*cols)*scale;food.y=Math.floor(Math.random()*rows)*scale}
placeFood();window.addEventListener('keydown',e=>{if(e.key===' '){paused=!paused;document.getElementById('status').textContent=paused?'PAUSED':'Press SPACE to pause/resume';return}
if(paused)return;switch(e.key){case 'ArrowUp':if(direction.y===0){direction={x:0,y:-1}}break;case 'ArrowDown':if(direction.y===0){direction={x:0,y:1}}break;case 'ArrowLeft':if(direction.x===0){direction={x:-1,y:0}}break;case 'ArrowRight':if(direction.x===0){direction={x:1,y:0}}break}});function draw(){if(paused)return;ctx.fillStyle='#000';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#f00';ctx.fillRect(food.x,food.y,scale,scale);const head={x:snake[0].x+direction.x*scale,y:snake[0].y+direction.y*scale};if(head.x<0||head.x>=canvas.width||head.y<0||head.y>=canvas.height||snake.some((s,i)=>i>0&&s.x===head.x&&s.y===head.y)){alert("Game Over! Score: "+score);snake=[{x:10*scale,y:10*scale}];direction={x:1,y:0};score=0;document.getElementById('score').textContent=score;placeFood();paused=!1;document.getElementById('status').textContent='Press SPACE to pause/resume';return}
snake.unshift(head);if(head.x===food.x&&head.y===food.y){score++;document.getElementById('score').textContent=score;placeFood()}else{snake.pop()}
ctx.fillStyle='#0f0';snake.forEach(segment=>ctx.fillRect(segment.x,segment.y,scale,scale))}
setInterval(draw,100)