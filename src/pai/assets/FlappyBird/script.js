document.getElementById("e").onclick = function () {
    location.href = "./E/run.html"; // Redirects to the parent directory and then to load.html
};
document.getElementById("n").onclick = function () {
    location.href = "./N/run.html"; // Redirects to the parent directory and then to load.html
};
document.getElementById("h").onclick = function () {
    location.href = "./H/run.html"; // Redirects to the parent directory and then to load.html
};