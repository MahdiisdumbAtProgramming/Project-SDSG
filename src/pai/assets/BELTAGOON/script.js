const canvas=document.getElementById('gameCanvas');const ctx=canvas.getContext('2d');const bulletImage=new Image();bulletImage.src='knife.png';const enemyImage=new Image();enemyImage.src='kris.png';const killSound=new Audio('kill.wav');const playerImage=new Image();playerImage.src='player.png';let imagesLoaded=0;bulletImage.onload=()=>{imagesLoaded++;if(imagesLoaded===32)startGame();};enemyImage.onload=()=>{imagesLoaded++;if(imagesLoaded===3)startGame();};playerImage.onload=()=>{imagesLoaded++;if(imagesLoaded===3)startGame();};const player={x:canvas.width/2-68/2,y:canvas.height-50,width:68,height:65,speed:10,bullets:[]};const keys={up:!1,down:!1,left:!1,right:!1};const enemies=[];let score=0;let highScore=localStorage.getItem('highScore')||0;let paused=!1;document.getElementById('highScore').textContent=highScore;function startGame(){update()}
function drawPlayer(){ctx.drawImage(playerImage,player.x,player.y,player.width,player.height)}
function drawBullets(){player.bullets.forEach((bullet,bIndex)=>{ctx.drawImage(bulletImage,bullet.x,bullet.y,bullet.width,bullet.height);bullet.y-=bullet.speed;if(bullet.y+bullet.height<0){player.bullets.splice(bIndex,1)}else{enemies.forEach((enemy,eIndex)=>{if(bullet.x<enemy.x+enemy.width&&bullet.x+bullet.width>enemy.x&&bullet.y<enemy.y+enemy.height&&bullet.y+bullet.height>enemy.y){enemies.splice(eIndex,1);player.bullets.splice(bIndex,1);score++;document.getElementById('score').textContent=score;killSound.play();if(score>highScore){highScore=score;localStorage.setItem('highScore',highScore);document.getElementById('highScore').textContent=highScore}}})}})}
function drawEnemies(){enemies.forEach((enemy,index)=>{ctx.drawImage(enemyImage,enemy.x,enemy.y,enemy.width,enemy.height);moveEnemy(enemy);checkCollisionWithPlayer(enemy,index);if(enemy.y>canvas.height||enemy.x>canvas.width||enemy.x+enemy.width<0){enemies.splice(index,1)}})}
function moveEnemy(enemy){const angle=Math.atan2(player.y-enemy.y,player.x-enemy.x);enemy.x+=enemy.speed*Math.cos(angle);enemy.y+=enemy.speed*Math.sin(angle)}
function checkCollisionWithPlayer(enemy,index){if(player.x<enemy.x+enemy.width&&player.x+player.width>enemy.x&&player.y<enemy.y+enemy.height&&player.y+player.height>enemy.y){alert('Kris touched you meaning the player now has control.');enemies.length=0;player.bullets.length=0;player.x=canvas.width/2-player.width/2;player.y=canvas.height-50;score=0;document.getElementById('score').textContent=score}}
function movePlayer(){if(keys.up&&player.y>0)player.y-=player.speed;if(keys.down&&player.y+player.height<canvas.height)player.y+=player.speed;if(keys.left&&player.x>0)player.x-=player.speed;if(keys.right&&player.x+player.width<canvas.width)player.x+=player.speed}
function drawPauseScreen(){ctx.fillStyle='rgba(0, 0, 0, 0.5)';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle='white';ctx.font='30px Arial';ctx.textAlign='center';ctx.fillText('Paused',canvas.width/2,canvas.height/2-20);ctx.font='20px Arial';ctx.fillText('Press / to continue',canvas.width/2,canvas.height/2+20)}
function update(){if(paused){drawPauseScreen();requestAnimationFrame(update);return}
ctx.clearRect(0,0,canvas.width,canvas.height);drawPlayer();drawBullets();drawEnemies();movePlayer();requestAnimationFrame(update)}
document.addEventListener('keydown',(e)=>{if(e.key==='ArrowUp')keys.up=!0;if(e.key==='ArrowDown')keys.down=!0;if(e.key==='ArrowLeft')keys.left=!0;if(e.key==='ArrowRight')keys.right=!0;if(e.key===' '){player.bullets.push({x:player.x+player.width/2-29/2,y:player.y,width:29,height:47,speed:14})}
if(e.key==='/'){paused=!paused;if(!paused){update()}}});document.addEventListener('keyup',(e)=>{if(e.key==='ArrowUp')keys.up=!1;if(e.key==='ArrowDown')keys.down=!1;if(e.key==='ArrowLeft')keys.left=!1;if(e.key==='ArrowRight')keys.right=!1});setInterval(()=>{if(!paused){const enemyWidth=19;const enemyHeight=37;const x=Math.random()*(canvas.width-enemyWidth);const speed=Math.random()*2+1;enemies.push({x,y:0,width:enemyWidth,height:enemyHeight,speed})}},1000)