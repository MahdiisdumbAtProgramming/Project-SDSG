const canvas=document.getElementById('gameCanvas');const ctx=canvas.getContext('2d');const resSound=new Audio('./revive.mp3');const players=[{x:50,y:canvas.height-60,width:40,height:40,speed:5,bullets:[],alive:!0,invincible:!1,invincibleTime:0,color:'red',controls:{up:'ArrowUp',down:'ArrowDown',left:'ArrowLeft',right:'ArrowRight',shoot:' '},score:0},{x:100,y:canvas.height-60,width:40,height:40,speed:5,bullets:[],alive:!0,invincible:!1,invincibleTime:0,color:'blue',controls:{up:'w',down:'s',left:'a',right:'d',shoot:'e'},score:0}];const keys={};const enemies=[];let totalScore=0;let highScore=localStorage.getItem('highScore')?parseInt(localStorage.getItem('highScore')):0;let paused=!1;let wave=1;function drawPlayer(player){ctx.fillStyle=player.invincible?'rgba(255,255,255,0.5)':player.color;ctx.fillRect(player.x,player.y,player.width,player.height);ctx.fillStyle='#fff';ctx.font='16px Arial';ctx.fillText(`Score: ${player.score}`,player.x,player.y-10)}
function drawHighScore(){ctx.fillStyle='#fff';ctx.font='20px Arial';ctx.textAlign='center';ctx.fillText(`High Score: ${highScore}`,canvas.width/2,30)}
function drawBullets(player){player.bullets.forEach((bullet,bIndex)=>{ctx.fillStyle='yellow';ctx.fillRect(bullet.x,bullet.y,bullet.width,bullet.height);bullet.y-=bullet.speed;if(bullet.y+bullet.height<0){player.bullets.splice(bIndex,1)}
enemies.forEach((enemy,eIndex)=>{if(bullet.x<enemy.x+enemy.width&&bullet.x+bullet.width>enemy.x&&bullet.y<enemy.y+enemy.height&&bullet.y+bullet.height>enemy.y){enemy.health-=5;player.bullets.splice(bIndex,1);if(enemy.health<=0){enemies.splice(eIndex,1);player.score++;totalScore++;if(totalScore>highScore){highScore=totalScore;localStorage.setItem('highScore',highScore)}}}})})}
function drawEnemies(){ctx.fillStyle='green';enemies.forEach((enemy,index)=>{ctx.fillRect(enemy.x,enemy.y,enemy.width,enemy.height);moveEnemy(enemy);checkCollisionWithPlayers(enemy,index);if(enemy.y>canvas.height||enemy.x>canvas.width||enemy.x<-enemy.width){enemies.splice(index,1)}
ctx.fillStyle='white';ctx.font='12px Arial';ctx.textAlign='center';ctx.fillText(enemy.health,enemy.x+enemy.width/2,enemy.y-5)})}
function moveEnemy(enemy){const alivePlayers=players.filter(p=>p.alive);if(alivePlayers.length===0)return;let closest=null;let minDist=Infinity;for(const p of alivePlayers){const dist=Math.hypot(p.x-enemy.x,p.y-enemy.y);if(dist<minDist){minDist=dist;closest=p}}
if(closest){const angle=Math.atan2(closest.y-enemy.y,closest.x-enemy.x);enemy.x+=enemy.speed*Math.cos(angle);enemy.y+=enemy.speed*Math.sin(angle)}}
function checkCollisionWithPlayers(enemy,enemyIndex){players.forEach((player)=>{if(player.alive&&!player.invincible&&player.x<enemy.x+enemy.width&&player.x+player.width>enemy.x&&player.y<enemy.y+enemy.height&&player.y+player.height>enemy.y){player.alive=!1;player.color='pink'}})}
function movePlayer(player){if(!player.alive)return;if(keys[player.controls.up]&&player.y>0)player.y-=player.speed;if(keys[player.controls.down]&&player.y+player.height<canvas.height)
player.y+=player.speed;if(keys[player.controls.left]&&player.x>0)player.x-=player.speed;if(keys[player.controls.right]&&player.x+player.width<canvas.width)
player.x+=player.speed}
function shoot(player){if(!player.alive)return;if(player.bullets.length>5)return;player.bullets.push({x:player.x+player.width/2-2.5,y:player.y,width:5,height:10,speed:7})}
function checkPlayerRevival(){players.forEach((player,index)=>{if(!player.alive){const otherPlayer=players[1-index];if(otherPlayer.alive&&otherPlayer.x<player.x+player.width&&otherPlayer.x+otherPlayer.width>player.x&&otherPlayer.y<player.y+player.height&&otherPlayer.y+otherPlayer.height>player.y){player.alive=!0;player.invincible=!0;player.invincibleTime=5000;resSound.play();player.color=index===0?'red':'blue'}}})}
function endGame(){alert("Game Over (Reload Page)");location.reload()}
function checkGameOver(){if(!players[0].alive&&!players[1].alive){endGame()}}
function drawPauseScreen(){ctx.fillStyle='rgba(0, 0, 0, 0.5)';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle='white';ctx.font='30px Arial';ctx.textAlign='center';ctx.fillText('Paused',canvas.width/2,canvas.height/2-20);ctx.font='20px Arial';ctx.fillText('Press / to continue',canvas.width/2,canvas.height/2+20)}
function drawWave(){ctx.fillStyle='white';ctx.font='20px Arial';ctx.textAlign='left';ctx.fillText(`Wave: ${wave}`,10,25)}
function update(){if(paused){drawPauseScreen();requestAnimationFrame(update);return}
ctx.clearRect(0,0,canvas.width,canvas.height);players.forEach(p=>{if(p.invincible){p.invincibleTime-=16;if(p.invincibleTime<=0){p.invincible=!1}}});drawHighScore();drawWave();players.forEach(drawPlayer);players.forEach(p=>drawBullets(p));drawEnemies();players.forEach(movePlayer);checkPlayerRevival();checkGameOver();if(enemies.length===0){wave++;spawnWave()}
requestAnimationFrame(update)}
document.addEventListener('keydown',(e)=>{keys[e.key]=!0;players.forEach(p=>{if(e.key===p.controls.shoot){shoot(p)}});if(e.key==='/'){paused=!paused;if(!paused)requestAnimationFrame(update);}});document.addEventListener('keyup',(e)=>{keys[e.key]=!1});setInterval(()=>{spawnWave()},15000);function spawnWave(){const enemyCount=3+wave;const enemyHealth=5+Math.floor(wave/2);for(let i=0;i<enemyCount;i++){const enemyWidth=40;const enemyHeight=40;const x=Math.random()*(canvas.width-enemyWidth);const speed=Math.random()*2+1+wave*0.2;enemies.push({x,y:0,width:enemyWidth,height:enemyHeight,speed,health:enemyHealth})}}
update()