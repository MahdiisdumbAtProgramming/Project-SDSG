const SIZE=9;const MINES=10;let cells=[],mines=new Set(),revealed=new Set(),flagged=new Set();let firstClick=!0,dead=!1,locked=!1;let time=0,timerId;const grid=document.getElementById("grid");const smiley=document.getElementById("smiley");const mineCounter=document.getElementById("mineCounter");const timer=document.getElementById("timer");const overlay=document.getElementById("bossOverlay");const introMusic=document.getElementById("introMusic");const bossMusic=document.getElementById("bossMusic");function reset(){grid.innerHTML="";cells=[];mines.calear();revealed.clear();flagged.clear();firstClick=!0;dead=!1;locked=!1;clearInterval(timerId);time=0;timer.textContent="000";mineCounter.textContent=String(MINES).padStart(3,"0");smiley.textContent="🙂";overlay.style.display="none";overlay.style.pointerEvents="";overlay.querySelectorAll(".spike").forEach(s=>s.remove());stopMusic();for(let i=0;i<SIZE*SIZE;i++){const c=document.createElement("div");c.className="cell";c.onmousedown=()=>{if(!dead&&!locked)smiley.textContent="😐"};c.onmouseup=c.onmouseleave=()=>{if(!dead&&!locked)smiley.textContent="🙂"};c.onclick=()=>reveal(i);c.oncontextmenu=e=>{e.preventDefault();flag(i)};grid.appendChild(c);cells.push(c)}}
function stopMusic(){introMusic.pause();bossMusic.pause()}
function startTimer(){timerId=setInterval(()=>{time++;timer.textContent=String(time).padStart(3,"0")},1000)}
function placeMines(safe){while(mines.size<MINES){let r=Math.floor(Math.random()*SIZE*SIZE);if(r!==safe)mines.add(r);}}
function neighbors(i){let x=i%SIZE,y=Math.floor(i/SIZE),n=[];for(let dx=-1;dx<=1;dx++)
for(let dy=-1;dy<=1;dy++){if(!dx&&!dy)continue;let nx=x+dx,ny=y+dy;if(nx>=0&&nx<SIZE&&ny>=0&&ny<SIZE)
n.push(ny*SIZE+nx);}
return n}
function reveal(i){if(dead||locked||revealed.has(i)||flagged.has(i))return;if(firstClick){placeMines(i);startTimer();firstClick=!1}
revealed.add(i);const c=cells[i];c.classList.add("revealed");if(mines.has(i)){c.textContent="💣";c.classList.add("mine");lose();return}
const count=neighbors(i).filter(n=>mines.has(n)).length;if(count){c.textContent=count;c.classList.add("n"+count)}else neighbors(i).forEach(reveal)}
function flag(i){if(dead||locked||revealed.has(i))return;const c=cells[i];if(flagged.has(i)){flagged.delete(i);c.textContent=""}else{flagged.add(i);c.textContent="🚩"}
mineCounter.textContent=String(MINES-flagged.size).padStart(3,"0")}
function lose(){dead=!0;clearInterval(timerId);smiley.textContent="☹️";mines.forEach(i=>{if(!revealed.has(i)){cells[i].textContent="💣";cells[i].classList.add("revealed")}})}
const konami=["ArrowUp","ArrowUp","ArrowDown","ArrowDown","ArrowLeft","ArrowRight","ArrowLeft","ArrowRight"];let buffer=[];window.addEventListener("keydown",e=>{buffer.push(e.key);buffer.splice(0,buffer.length-konami.length);if(konami.every((k,i)=>buffer[i]===k))triggerBoss();});function triggerBoss(){reset();locked=!0;overlay.style.display="flex";overlay.style.pointerEvents="auto";const CIRCLE_RADIUS=50;const SPIKE_HEIGHT=5;for(let i=0;i<9;i++){const spike=document.createElement("div");spike.className="spike";overlay.appendChild(spike)}
let angleOffset=0;function spinSpikes(){angleOffset+=2;overlay.querySelectorAll(".spike").forEach((spike,i)=>{const angle=(360/9)*i+angleOffset;spike.style.transform=`translate(-50%,-50%) rotate(${angle}deg) translateY(-${CIRCLE_RADIUS}px) translateY(-${SPIKE_HEIGHT/2}px)`});if(overlay.style.display==="flex")requestAnimationFrame(spinSpikes);}
spinSpikes();introMusic.currentTime=0;introMusic.onended=()=>{overlay.querySelectorAll(".spike").forEach(s=>s.remove());overlay.style.pointerEvents="none";bossMusic.currentTime=0;bossMusic.play().catch(()=>{});locked=!1;startBoss()};const playPromise=introMusic.play();if(playPromise!==undefined){playPromise.catch(()=>{introMusic.onended()})}}
let bossPos={x:4,y:4};function regenerateMinesAvoidingRevealed(){mines.clear();while(mines.size<MINES){let r=Math.floor(Math.random()*SIZE*SIZE);if(revealed.has(r))continue;mines.add(r)}
mineCounter.textContent=String(MINES-flagged.size).padStart(3,"0")}
function refreshRevealedCounts(){for(const i of revealed){const c=cells[i];if(c.classList.contains("mine"))continue;for(let n=1;n<=8;n++)c.classList.remove("n"+n);const count=neighbors(i).filter(n=>mines.has(n)).length;if(count)c.textContent=count,c.classList.add("n"+count);else c.textContent=""}}
function clearBossVisuals(){for(let dx=-1;dx<=1;dx++)
for(let dy=-1;dy<=1;dy++){const x=bossPos.x+dx,y=bossPos.y+dy;if(x>=0&&x<SIZE&&y>=0&&y<SIZE){const idx=y*SIZE+x;cells[idx].classList.remove("boss","boss-center");if(!revealed.has(idx)&&!flagged.has(idx))cells[idx].textContent=""}}}
function startBoss(){const BOSS_SIZE=3;function moveBoss(){if(overlay.style.display!=="flex")return;clearBossVisuals();bossPos.x=Math.floor(Math.random()*(SIZE-2));bossPos.y=Math.floor(Math.random()*(SIZE-2));regenerateMinesAvoidingRevealed();for(let dx=-1;dx<=1;dx++)
for(let dy=-1;dy<=1;dy++){const x=bossPos.x+dx,y=bossPos.y+dy;if(x>=0&&x<SIZE&&y>=0&&y<SIZE){const idx=y*SIZE+x;cells[idx].classList.add("boss");if(dx===0&&dy===0){cells[idx].classList.add("boss-center");if(!revealed.has(idx)&&!flagged.has(idx))cells[idx].textContent="9"}}}
refreshRevealedCounts();setTimeout(moveBoss,1000)}
regenerateMinesAvoidingRevealed();moveBoss()}
smiley.onclick=reset;reset()