const player=document.getElementById('player');const target=document.getElementById('target');const scoreElement=document.getElementById('score');const highscoreElement=document.getElementById('highscore');const gameContainer=document.getElementById('game-container');const containerWidth=gameContainer.offsetWidth;const containerHeight=gameContainer.offsetHeight;let score=0;let highscore=localStorage.getItem('highScore')||0;let playerSpeed=4.5;let targetSpeed=4;highscoreElement.textContent=highscore;let playerX=100;let playerY=100;let targetX=Math.random()*(containerWidth-60);let targetY=Math.random()*(containerHeight-60);let targetVelocityX=targetSpeed*(Math.random()>0.5?1:-1);let targetVelocityY=targetSpeed*(Math.random()>0.5?1:-1);let previousTargetX=targetX;let previousTargetY=targetY;let stuckCounter=0;const maxStuckFrames=30;const keys={ArrowUp:!1,ArrowDown:!1,ArrowLeft:!1,ArrowRight:!1};window.addEventListener('keydown',(e)=>{if(keys.hasOwnProperty(e.key)){keys[e.key]=!0}});window.addEventListener('keyup',(e)=>{if(keys.hasOwnProperty(e.key)){keys[e.key]=!1}});function moveTargetRandomly(){targetX+=targetVelocityX;targetY+=targetVelocityY;if(targetX<=0||targetX>=containerWidth-60){targetVelocityX*=-1;targetX=Math.max(0,Math.min(containerWidth-60,targetX))}
if(targetY<=0||targetY>=containerHeight-60){targetVelocityY*=-1;targetY=Math.max(0,Math.min(containerHeight-60,targetY))}}
function teleportTarget(){targetX=Math.random()*(containerWidth-60);targetY=Math.random()*(containerHeight-60);targetVelocityX=targetSpeed*(Math.random()>0.5?1:-1);targetVelocityY=targetSpeed*(Math.random()>0.5?1:-1)}
function update(){if(keys.ArrowUp&&playerY>0)playerY-=playerSpeed;if(keys.ArrowDown&&playerY<containerHeight-60)playerY+=playerSpeed;if(keys.ArrowLeft&&playerX>0)playerX-=playerSpeed;if(keys.ArrowRight&&playerX<containerWidth-60)playerX+=playerSpeed;player.style.left=playerX+'px';player.style.top=playerY+'px';previousTargetX=targetX;previousTargetY=targetY;const distanceToPlayer=Math.sqrt((targetX-playerX)**2+(targetY-playerY)**2);if(distanceToPlayer<200){const deltaX=targetX-playerX;const deltaY=targetY-playerY;const distance=Math.sqrt(deltaX*deltaX+deltaY*deltaY);targetVelocityX=(deltaX/distance)*targetSpeed;targetVelocityY=(deltaY/distance)*targetSpeed;targetX+=targetVelocityX;targetY+=targetVelocityY;targetX=Math.max(0,Math.min(containerWidth-60,targetX));targetY=Math.max(0,Math.min(containerHeight-60,targetY))}else{moveTargetRandomly()}
if(targetX===previousTargetX&&targetY===previousTargetY){stuckCounter++;if(stuckCounter>=maxStuckFrames){teleportTarget();stuckCounter=0}}else{stuckCounter=0}
target.style.left=targetX+'px';target.style.top=targetY+'px';if(Math.abs(playerX-targetX)<60&&Math.abs(playerY-targetY)<60){score++;scoreElement.textContent=score;if(score>highscore){highscore=score;localStorage.setItem('highScore',highscore);highscoreElement.textContent=highscore}
repositionTarget()}
requestAnimationFrame(update)}
function repositionTarget(){targetX=Math.random()*(containerWidth-60);targetY=Math.random()*(containerHeight-60);stuckCounter=0;targetVelocityX=targetSpeed*(Math.random()>0.5?1:-1);targetVelocityY=targetSpeed*(Math.random()>0.5?1:-1)}
update()