const fs=require('fs');const path=require('path');const normal='./pai/assets/';const premium='./pai/PREMIUM/assets/huh/stealer leave/there is nothing pay then you get/what are you doing/STOP/i cant understand/security left';const directories=fs.readdirSync(normal,{withFileTypes:!0}).filter(dirent=>dirent.isDirectory()).map(dirent=>dirent.name);const games=[];const premiumGames=[];console.log('Scanning directories...\n');directories.forEach(dir=>{const runPath=path.join(normal,dir,'run.html');if(fs.existsSync(runPath)){games.push(dir);console.log(`✅ Found run.html in "${dir}"`)}else{console.log(`❌ No run.html in "${dir}"`)}});const premiumDirectories=fs.readdirSync(premium,{withFileTypes:!0}).filter(dirent=>dirent.isDirectory()).map(dirent=>dirent.name);premiumDirectories.forEach(dir=>{const runPath=path.join(premium,dir,'run.html');if(fs.existsSync(runPath)){premiumGames.push(dir);console.log(`✅ Found run.html in PREMIUM "${dir}"`)}else{console.log(`❌ No run.html in PREMIUM "${dir}"`)}});const currentDir=path.resolve(__dirname);const output=`// Auto-generated list of games
const games = ${JSON.stringify(games, null, 4)};
const premiumGames = ${JSON.stringify(premiumGames, null, 4)};
const gameschk = ${JSON.stringify(games, null, 4)};
const premiumGameschk = ${JSON.stringify(premiumGames, null, 4)};`;fs.writeFileSync(path.join(currentDir,'games.js'),output,'utf8');console.log(`\nDone! games.js updated with ${games.length} normal games and ${premiumGames.length} premium games.`)